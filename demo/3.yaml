folder: media/demo_official/3
select:
  name: Local
  config:
    source: data/demo/3video
    aggregate: true
analyse:
  - name: Frames
    config:
      dev: true
  - name: ImageDedup
    config:
      threshold: 3
      method: dhash

