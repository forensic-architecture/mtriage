desc: Split into frames using opencv, skipping duplicate frames. Note that this approach takes significantly longer than the regular frames analyser, which uses FFMPEG.
args:
  - name: change_threshold
    required: false
    input: string
  - name: sequential
    required: false
    input: bool
  - name: fps
    required: false
    input: int
