folder: media/pytorchfasterrcnn
select:
  name: Local
  config:
    source: data/images
    aggregate: true
analyse:
  name: PytorchFasterRcnn
  config:
    dev: true
    model: data/sean1.pth
    class_map:
      - background
      - canister
      - cylinder
      - can
      - bottle
      - bin


